# v0.3.0 验证记录与 Windows 实测

## 本次已完成

- .NET SDK 8.0.425 下核心测试运行通过：既有99项、v0.2.1文档54项、gap检查24项、本次音频/时间线/MIDI30项，共207项。
- 主程序 Windows 目标 Release 编译通过，0警告、0错误。
- WindowsSmoke 测试项目 Release 编译通过，0警告、0错误。
- 本次自动测试涵盖 C3/C4/C5/C6及升半音、附点/减时/自定义拍数、gap与休止静音、分块合成连续性、音量、A4频率、光标位置、SMF结构、BPM、音色指令、配对音符事件、末尾休止、累计时间精度、原子写入失败保护及旧设置兼容。
- Windows窗口测试新增：从光标试听、编辑锁定、游戏/试听互斥、进度高亮、音量、停止恢复光标、音频错误后重试、完成恢复、播放中关闭、MIDI导出及取消时保留TXT未保存状态。

## 尚未在此环境运行

当前为 Linux 环境，不能运行 WPF 窗口与 WinMM 音频设备。WindowsSmoke 仅完成编译；该套测试运行时使用可控音频替身，不检验真实扬声器/声卡。游戏演奏、本地实际音质、设备拔插及Windows实际界面仍需实机确认。不要把编译通过当作这些项目通过。

## Windows 构建

覆盖源码时保留自己的“简谱”目录和Git信息。本源码包不携带个人简谱和历史曲谱ZIP，不会要求删除它们。

```powershell
cd D:\Downloads\HarmonicaPlayer
powershell -NoProfile -ExecutionPolicy Bypass -File .\Build-Release.ps1
```

脚本保留既有自动测试门禁，自动执行核心测试与Windows窗口测试后生成：
`bin\Release\HarmonicaPlayer-v0.3.0-win-x64.zip`

打包仍只复制“简谱”目录内的TXT，保留新的audio-midi-demo.txt示例，不嵌套曲谱ZIP。无需手动重做上一轮全套测试。

## 本版新增功能手动检查（约5分钟）

1. 用普通权限打开新EXE，导入audio-midi-demo.txt。点击“从头试听”，应立即发声（非游戏倒计时）；前四音逐个高八度，休止静音；音符高亮/进度更新，末尾休止结束后到100%。整个示例为16.625拍，120 BPM约8.313秒。
2. 将光标放到第二行的高音上，“从光标试听”应由该音播放到曲末。试听中曲谱/BPM/间隔/文件按钮锁定，游戏开始不可用；音量调至0静音、调回恢复，游戏音量不变。
3. 点击“停止”或有效停止热键，声音及时结束且编辑恢复，光标回到试听前的位置。修改一个音再试听，应使用修改结果。不要重测与本版无关的旧谱用例。
4. 试听中关闭窗口，应停止声音；未保存时选“取消”应留在窗口并可继续编辑/试听。重启软件，试听音量应保留。
5. 编辑后不保存TXT直接“导出 MIDI”，标题星号应保留。用支持MIDI的外部软件打开，确认能播放、BPM正确、八度/升音/长短音/休止/总时长一致；音色允许不同。导出取消后也应保持原编辑状态。
6. 若方便，在无有效输出设备时尝试试听，应明显报错且可恢复编辑。恢复默认输出设备后可重试。设备异常项目尚未实测。

TXT格式保持HarmonicaPlayer/1。播放列表、暂停、动态BPM、MIDI导入仍不在范围内。此包为Windows实测候选源码，未发布远端Release或处理PR。
