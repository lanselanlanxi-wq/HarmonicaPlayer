# v0.2.0 修正版源码候选包

覆盖项目内同名文件，保留自己的“简谱”文件夹。不要删除整个项目再覆盖。
旧 UPDATE-0.1.x.md 是历史说明，可自行保留，不影响运行；本包不再附带。

本次修正：
- 曲谱校验失败和停止热键不可用不再复制成残留操作错误，问题解决后恢复提示状态。
- 增加错误字符定位与高亮，开始按钮按曲谱及停止键可用性更新，并明确显示原因。
- 打包恢复复制本地“简谱”文件夹，缺少文件夹时提示，不创建或覆盖用户曲谱。
- 使用说明重写为普通用户操作及常见问题。
- 删除旧模式控件、播放分支、额外空白停顿、解析开关和废弃设置字段。
- 读取旧设置时忽略废弃字段，保留有效快捷键、BPM和音符间隔。
- 增加设置迁移与 Windows 界面错误恢复、定位、播放按钮状态回归用例。

验证范围：完成源码静态检查和压缩包完整性检查。当前环境没有 .NET SDK，重新下载连接超时，因此未运行编译、核心测试或 Windows 界面测试，不宣称测试通过。

Windows 下执行：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\Build-Release.ps1
```
脚本会先运行核心测试和 Windows 界面测试，通过后才发布压缩包。请继续实测游戏里的右键＋逗号音高、快捷键停止和关闭窗口。
