# v0.2.1 更新说明

- 新增保存TXT、另存为、新建及曲名编辑，导出文件包含曲名、BPM和音符间隔。
- 自动读取@gap（毫秒）；未写默认20，旧谱也一样，不沿用上一首的间隔。修改间隔会标记为未保存。
- 导入自动读取BPM：文件头优先，其次文件名；未标注时提示确认当前速度。
- 保留旧纯音符TXT兼容，首次保存必须另存，修改BPM不自动改文件名。
- 星号标记未保存修改，关闭和换谱前可保存/放弃/取消。
- 修正异步保存的过期界面提示；保留顺序写入和失败重试。
- 预览、播放和导出共用时长校验，保留最短音保护和错误定位。
- 增加格式、保存和Windows回归测试，更新AI转谱模板、制谱说明和示例。

感谢 daailiuying（PR #3）和 izumikonata10（PR #1），采纳范围见 CONTRIBUTIONS.md。

新文件头需v0.2.1读取；本次仍没有本地声音试听、MIDI或播放列表。窗口和游戏行为必须在Windows验证后才能正式发布。
