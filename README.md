# HarmonicaPlayer v0.3.3

Windows 单音简谱播放器：编辑和保存 TXT、本地声音试听、导出标准 MIDI，以及通过模拟键鼠在游戏内演奏。

基于 v0.3.2，改进连续短音试听。用户已于2026年9月25日确认实机测试无问题。验证范围见 VALIDATION-0.3.3.md。

## v0.3.3 更新

- 有效音长不超过250ms时，从录音200ms处起播，配合12ms平滑淡入，让短音更快形成稳定音色。
- 250～500ms逐渐恢复录音原始起音，避免时长阈值造成突变。
- 不超过300ms的短音不切换Release；300～700ms逐渐增加真实尾音比例，700ms及以上保留完整尾音衔接。
- 音符末端用20～35ms平滑淡出，短音按比例缩短；保留gap、休止和总时长，重复同音仍分别发声。
- 新增用户反馈片段在BPM90/120/180的65项检查，支持括号内外两种时值写法。
- AudioExamples附同一片段v0.3.2/v0.3.3的离线WAV（BPM120、gap20、音量60%），可先比较听感，再构建程序。
- 采样仍嵌入EXE；完整覆盖源码时包括Assets。MIDI与游戏输入规则未改。

## 保留的 v0.3.0 功能

- 本地真实口琴采样试听：从头或从光标位置开始，无倒计时，不发送键鼠、不需要管理员权限。
- 当前音符高亮、进度及剩余时间；停止后恢复原编辑光标，使用修改后的最新内容重新试听。
- 试听音量 0～100%，默认 60%，单独保存为软件设置，播放中可调，不写入 TXT 或 MIDI。
- 导出整首曲谱为标准单轨 MIDI（.mid），保留音高、BPM、长短音、间隔、休止及末尾休止。
- 试听、MIDI、游戏演奏、解析检查及 TXT 保存使用同一最短时长校验。
- 试听与游戏演奏互斥，试听中锁定曲名、正文、BPM、间隔和文件操作；停止或错误后恢复。
- 沿用 v0.2.1 的 TXT 文件头、旧谱兼容、自动 BPM/@gap、未保存保护和异步设置保存修复。

贡献来源与历史采纳范围见 [CONTRIBUTIONS.md](CONTRIBUTIONS.md)。v0.3.0 音频与 MIDI 为本次独立实现；未操作远端 PR。

## 本地试听与 MIDI

导入 TXT 后点击“从头试听”；需要核对局部时，把光标放到正文的音符上，点击“从光标试听”。光标在空白、小节线或音符前的括号/#上时从下一音开始；在时值符号或结束括号上时从所属音开始；正文末尾从最后一音开始。会试听到曲末，不循环。停止按钮或已注册的停止快捷键均可停止；快捷键冲突不妨碍按钮试听和停止。

中音1=C4（MIDI 60）、低音1=C3、高音1=C5、最高do=C6；#升半音。原谱调号不会自动移调。试听采用真实口琴录音，但不保证与游戏音色或绝对音高一致。

每音按谱面拍数占用时间，末尾扣除当前 gap 作为留白，不在总时长之外加间隔。休止按完整时值静音。游戏输入需要的12ms按键准备不作为试听/MIDI的前置静音；三种方式的拍点和总时长一致，游戏按住时间额外少12ms。为保持游戏可演奏性，所有入口仍执行原12ms准备＋40ms按住的校验。

“导出 MIDI”始终导出当前编辑中的整首曲谱，不受光标位置影响；不用先保存 TXT，也不会清除标题星号。使用标准 MIDI Format 0、单轨、9600 PPQ、一个固定速度、GM口琴音色（22号，程序号21）、固定力度96。MIDI不含采样音频，外部播放器音色可能不同；试听音量不影响导出。导出先写临时文件再替换，失败保留已有目标。未定义拍号，不向MIDI猜填拍号。外部软件可能把未标拍号的文件显示为默认4/4，这不改变音符时值。

暂不包含暂停、播放列表、MIDI导入、动态BPM或和弦。TXT继续使用 HarmonicaPlayer/1，无需转换旧谱。

## 使用

解压发布包，运行HarmonicaPlayer.exe，导入TXT并确认速度（BPM）。默认“仅日志测试”不发声、不发送键鼠。实际演奏需取消勾选，进入游戏口琴界面，按开始键（默认F6），停止默认F8。

停止键冲突时禁止实际演奏；开始键冲突时仍可点开始按钮，在3秒倒计时内切入游戏。权限应与游戏匹配，需要时以管理员运行；游戏建议无边框模式。演奏时除停止键外不要操作键鼠。

## TXT格式

旧纯音符TXT仍可导入。文件名可写“歌名 bpm100.txt”“歌名_BPM100.txt”“歌名-bpm100.txt”“歌名 bpm 100.txt”。BPM为20～300整数。

建议在软件内保存新格式：

```text
@format=HarmonicaPlayer/1
@title=节奏示例
@bpm=120
@gap=20

1 2_ 3_ 5 — |
0 6. 5_ 1 |
```

- 文件格式版本与软件版本独立。新格式要求@format和@bpm；@title可省略，导入时用文件名。界面曲名不能为空。
- 文件头与正文之间必须有空行。同名字段不能重复（不区分大小写）。未知字段提示后原样保留，但不解释其作用，例如@time不会改变节拍。
- 文件头与文件名BPM冲突时以文件头为准并提示。没有BPM时沿用界面有效速度，界面无效则使用最后有效设置。
- @gap可省略，省略时使用20ms（旧纯音符TXT也一样）。填写时必须是10～5000的整数；空值、越界、重复字段会报错。保存时自动写入当前间隔。间隔不会改变每拍总时长，但会缩短按住时间，仍受最短音校验限制。
- UTF-8，最大1MB。文件头不要粘贴进音符编辑区。
- 保存前检查曲谱、速度和间隔；先写同目录临时文件再替换目标。验证或写入失败不覆盖已有目标。
- v0.2.0及更早版本不识别新文件头；旧版兼容请单独复制正文到TXT，并在旧版设置BPM。

## 音符规则

| 写法 | 含义 |
|---|---|
| 1～7 | 中音，一拍 |
| 【1】～【7】 | 高一个八度 |
| （1）或(1) | 低一个八度 |
| 【【1】】 | 高两个八度do，仅此音 |
| #1、【#1】 | 升半音 |
| 0 | 休止一拍 |
| 5_、5__ | 半拍、四分之一拍 |
| 5.、5_. | 1.5拍、0.75拍 |
| 5 —、0 — | 增加一拍 |
| 5:1.25 | 同一个音总共持续1.25拍 |

冒号指定总拍数，大于0且不超过64，不可再混用下划线、附点或延长符。最高do的时值写在括号外，如【【1】】:1.25。空格、换行、小节线不增加停顿；小节线不执行满拍检查。扣除12ms准备和间隔后至少按住40ms，否则降低BPM或减小间隔。

完整写法见“制谱说明.txt”，AI提示词见“ai转谱模板.txt”，操作问题见“使用说明.txt”。

## 测试与打包（Windows + .NET 8 SDK）

```powershell
dotnet run --project .\Tests\ParserTests.csproj -c Release
dotnet run --project .\Tests\WindowsSmoke\WindowsSmokeTests.csproj -c Release
powershell -NoProfile -ExecutionPolicy Bypass -File .\Build-Release.ps1
```

脚本在测试失败时停止，生成bin\Release\HarmonicaPlayer-v0.3.3-win-x64.zip。包含自包含EXE、说明、模板、新旧示例和项目“简谱”文件夹内的TXT曲谱；ZIP等其他文件不会嵌套打包，没有曲谱文件夹时明确警告。

发布前只需重点听取本版短音、普通音和长音的收尾；试听控制与MIDI沿用v0.3.0。上一轮已做的手动测试无需整套重做。自动脚本保留已有回归门禁，并加入本版测试；Windows UI 测试用可控音频替身，不会发声，也不发送游戏音符按键。详细步骤见 VALIDATION-0.3.3.md。
