# 0.1.2 覆盖更新说明

基于 0.1.1。关闭程序，将压缩包全部文件解压至 HarmonicaPlayer.csproj 所在目录，覆盖同名文件。不要放进 bin 或 publish 目录。本包是源码更新，需要重新编译，不能直接覆盖 EXE。

## Windows 编译

在已安装 .NET 8 SDK 的开发环境、项目目录中执行：

```powershell
dotnet run --project Tests/ParserTests.csproj
dotnet build -c Release
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true
```

打开新生成的 bin/Release/net8.0-windows/win-x64/publish/HarmonicaPlayer.exe。

## 使用

默认保留0.1.1固定时长模式。使用带时值的谱子时勾选“节奏模式”，设置速度（默认四分音符每分钟120个）。导入 rhythm-demo.txt，预览应为16拍、8秒，不含3秒倒计时。

TXT导入仍为UTF-8，文件只包含谱面，不含标题、歌词、BPM或拍号声明。输入或导入后自动刷新解析预览，也可点击“预览 / 检查”。预览为时间列表，显示音高、拍数、时长和总时长；本版没有五线谱/标准简谱图形排版、声音试听或图片识谱。

| 文本 | 时值（四分音符为一拍） |
| --- | --- |
| 5 | 1拍 |
| 5 - 或 5 — | 2拍，持续同一个音，不重新起音 |
| 5 — — | 3拍 |
| 5_ | 半拍，下划线写在数字后面 |
| 5__ | 四分之一拍 |
| 5. | 1.5拍 |
| 5_. | 0.75拍 |
| 0 | 休止1拍 |
| 0_ | 休止半拍 |
| 0 — | 休止2拍 |
| 【1】 / （1） | 高音 / 低音，沿用原格式 |
| #1 | 升半音，沿用原格式 |
| | | 小节分隔，不额外增加时间 |

下划线、附点都是本程序的文本表示；不会直接识别图片中的下划线或上下点。时值符号顺序为减时符、附点、延长符；最多两条减时符、一个附点、单事件最多64拍。小节分隔只用于阅读，本版不校验每小节拍数，不支持连音线、三连音、变速或和弦。

节奏模式下空格和换行只用于排版，停顿请写0；旧模式的每音时长和分段停顿继续保留。每个音的尾部留白包含在该音时值内，不延长总时长；休止期间不发送输入。最短音减去留白必须至少40ms，否则程序提示降低速度或减少留白。

F6开始、F8停止、切离目标窗口停止、结束释放按键的原有逻辑保留。仅日志模式不会发声，也不发送输入。长音能否持续发声仍取决于游戏对长按的响应。

## 验证状态

附带原有解析测试和新增时值、非法输入、休止及时间换算测试。制作环境没有.NET SDK，未执行编译、C#测试、Windows界面或游戏实测。请先执行上面的测试和编译命令，再使用仅日志模式确认。
