# v0.3.1 验证

## 已完成

- 核心检查共255项通过：既有207项（音频测试适配真实采样），新增48项。
- 新增覆盖全部37个半音（C3～C6）的稳定段实际音高、长音多次循环、分块一致性、重复同音重新起音、静音/末尾gap及6个录音锚点循环接缝。
- 音高按录音稳定段检测。真实吹奏的起音有自然波动，不将瞬间频率当作持续音高；稳定段检查容差为10音分。
- 主程序与Windows窗口测试项目均通过Release编译，0警告、0错误。

## 本次未运行

本环境为Linux，不能执行WinMM实际出声或WPF窗口测试。上一版界面功能由用户确认正常，本次没有更改其流程。真实采样的主观听感需要用户在Windows上确认，测试通过不代表音色与游戏一致。

## Windows构建和重点试听

1. 将完整源码覆盖原项目，务必包括Assets/Harmonica文件夹；保留自己的简谱与Git信息。
2. 执行Build-Release.ps1，自动测试与编译通过后生成bin/Release/HarmonicaPlayer-v0.3.1-win-x64.zip。
3. 打开新EXE，用自己熟悉的曲谱试听普通音、低音和高音，再用一个长音（例如1:16）确认循环持续自然。
4. 检查停止和音量即可。前一轮已通过的导入/保存/MIDI等手动测试无需整套重做。

定向采样测试：
    dotnet run --project .\Tests\ParserTests.csproj -c Release -- --sample-audio-only

采样已编进EXE，发布包无需外置WAV，但会附来源和CC0授权文本。MIDI文件仍不包含录音。
